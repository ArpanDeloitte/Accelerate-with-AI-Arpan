# ============================================================
#  QueryGenie  ::  all runtime dependencies
#  Install:  pip install -r requirements.txt
#  (both launchers auto-install this on first run)
# ------------------------------------------------------------
#  Web app  (QueryGenie-Web.bat -> web_app.py, serves the UI on :8000)
fastapi>=0.110.0
uvicorn>=0.29.0
#  Live DB bridge  (start-bridge.bat -> app.py, connects to your Oracle DB on :8788)
oracledb>=2.4.0
Flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
# ============================================================
